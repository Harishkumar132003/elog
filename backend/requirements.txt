fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0
orjson==3.10.13
# Uploads: FastAPI raises at import time on an UploadFile route without this.
python-multipart==0.0.20

# MongoDB (Atlas — dnspython is required for mongodb+srv:// URIs)
motor==3.6.0
pymongo==4.9.2
dnspython==2.7.0

# Auth
pyjwt==2.10.1
bcrypt==4.2.1

# Corti API
httpx==0.28.1
# Speech-to-text is a WebSocket, not REST. Present already via uvicorn[standard],
# but pinned here because a transitive dependency is not a contract — the Docker
# build installs strictly from this file.
websockets==17.0.1

# Competency import — pure python, so no system libraries to install (PyMuPDF needs them)
pypdf==5.1.0
